# =============================================================================
# Clone Hero Content Manager - Environment Configuration
# =============================================================================
# Copy this file to .env and update values for your environment.
#
#   cp .env.example .env
#
# Lines starting with # are comments and are ignored.
# =============================================================================

# ---------------------------------------------------------------------------
# Application Settings
# ---------------------------------------------------------------------------

# Host and port for the web server
APP_HOST=0.0.0.0
APP_PORT=8000

# Application environment: "development" or "production"
APP_ENV=development

# Version string shown in the UI and health endpoint
APP_VERSION=1.0.0

# Enable debug mode (verbose logging, /docs endpoint, auto-reload)
# Set to "false" in production
DEBUG=true

# Secret key for session signing — CHANGE THIS in production
SECRET_KEY=change-me-to-a-random-string

# Timezone
TZ=America/Toronto

# ---------------------------------------------------------------------------
# Data & Content Paths
# ---------------------------------------------------------------------------

# Root directory for all persistent data (database, logs, content)
# When running in Docker this is mounted from the host via docker-compose.yml
DATA_DIR=/app/data

# Base directory for Clone Hero content (songs, backgrounds, etc.)
# Defaults to DATA_DIR/clonehero_content if not set
# CONTENT_DIR=/app/data/clonehero_content

# ---------------------------------------------------------------------------
# Logging
# ---------------------------------------------------------------------------

# Directory where log files are written
LOG_DIR=/app/data/logs

# Minimum log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=DEBUG

# Maximum size of a single log file before rotation (e.g. "10MB", "50MB")
LOG_FILE_SIZE=10MB

# How long to keep rotated log files (e.g. "7 days", "4 weeks")
LOG_RETENTION=7 days

# ---------------------------------------------------------------------------
# Nextcloud WebDAV Integration
# ---------------------------------------------------------------------------
# Connect to a Nextcloud instance to centralize your Clone Hero content.
# All three values are required for the integration to be active.
# Leave them empty/commented out to run in local-only mode.

# Base URL of your Nextcloud instance (no trailing slash)
# Example: https://cloud.example.com
NEXTCLOUD_URL=

# Nextcloud username
NEXTCLOUD_USERNAME=

# Nextcloud password or App Password (recommended)
# Generate an App Password at: Settings > Security > Devices & sessions
NEXTCLOUD_PASSWORD=

# Remote path template for the WebDAV endpoint
# {username} is replaced with NEXTCLOUD_USERNAME automatically
# Change the trailing path to match where you store Clone Hero content
NEXTCLOUD_REMOTE_PATH=/remote.php/dav/files/{username}/CloneHero

# ---------------------------------------------------------------------------
# Upload Limits
# ---------------------------------------------------------------------------

# Maximum upload file size in gigabytes
MAX_FILE_SIZE_GB=10
